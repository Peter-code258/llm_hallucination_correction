# 核心依赖
python>=3.8

# LLM和API客户端
openai>=1.0.0
requests>=2.25.0

# 向量数据库和嵌入
chromadb>=0.4.0
sentence-transformers>=2.2.0
torch>=1.9.0

# 数据处理和序列化
numpy>=1.21.0
pandas>=1.3.0
pyyaml>=6.0

# 文本处理
nltk>=3.6.0
regex>=2022.3.0

# 配置管理
pydantic>=1.9.0
python-dotenv>=0.19.0

# 测试框架
pytest>=6.2.0

# 自动 prompt 优化（DSPy）
# 用于 src/llm/dspy_signatures.py / dspy_adapter.py / prompt_optimizer.py / scripts/optimize_prompts.py
# 注：3.x 起 BootstrapFewShotWithMetric 已移除，本仓库改用 BootstrapFewShotWithRandomSearch
dspy>=3.0.0
litellm>=1.30.0  # DSPy 的 LM 后端依赖

# 评估指标
# 用于 PromptOptimizer._rouge_l（initial_answer / answer_correction 的默认 metric）
# 注：包不可用时 PromptOptimizer 会自动回退到 token-overlap，非硬依赖
rouge-score>=0.1.2

# Web框架（可选）
fastapi>=0.68.0
uvicorn>=0.15.0